import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('BioVault app smoke test placeholder', (WidgetTester tester) async {
    // Placeholder test — biometric auth requires real device hardware.
    // Add integration tests here once backend is ready.
    expect(true, isTrue);
  });
}
